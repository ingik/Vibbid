import { FunctionComponent } from 'react';
import { RootStackNavigation } from './src/navigations';

const AppContents: FunctionComponent = function AppContents() {
  return <RootStackNavigation />;
};

export default AppContents;
